import React from "react";
import PageTemplate from "../pageTemplate/PageTemplate";

const SearchEngine = () => {
  return (
    <div>
      <PageTemplate currentPage="search-engine">SearchEngine</PageTemplate>
    </div>
  );
};

export default SearchEngine;
